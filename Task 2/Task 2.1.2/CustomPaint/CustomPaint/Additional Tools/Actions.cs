namespace CustomPaint
{
    /// <summary>
    /// Enumeration of all actions that available to perform in figure editing menu.
    /// </summary>
    public enum Actions
    { 
        ChooseUser = 0,
        Show = 1,
        Add = 2,
        Remove = 3,
        Clear = 4
    }
}